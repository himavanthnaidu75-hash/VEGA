[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && pip install -e ."

[deploy]
startCommand = "uvicorn vega.backend.main:app --host 0.0.0.0 --port 8000"
restartPolicyType = "on_failure"